grammar SQLOrderByClause
  include SQLPrimitives
  include SQLRowSupport

  rule order_by
    "ORDER" space+ "BY" space+ one_or_more_column_names { 
      def eval
        CachedModel::Expression::OrderBy.new(one_or_more_column_names.eval)
      end
    }
  end
end
